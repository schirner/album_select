FROM python:3.14-slim
WORKDIR /workspace
RUN apt-get update && apt-get install -y --no-install-recommends \
      git gcc libffi-dev && rm -rf /var/lib/apt/lists/*
COPY requirements_test.txt .
RUN pip install --no-cache-dir -r requirements_test.txt
